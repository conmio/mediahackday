package com.conmio.mediahackday.article/** * Created with IntelliJ IDEA. * User: haren * Date: 10/5/13 * Time: 5:12 PM * To change this template use File | Settings | File Templates. */class Article {    String headline    String byline    String summary    String location    String shortUrl    Map<String,String> images    Date pubDate    /*    Article(articleJson) {        def selectSafe = { json, key, defVal = null ->            if (json?.has(key)) {                return json.get(key)            } else {                return defVal            }        }        this.headline = selectSafe(articleJson, "title_clean")        this.byline = selectSafe(articleJson, "rights_text")        this.summary = selectSafe(articleJson, "summary:")        this.location = selectSafe(articleJson, "location")        this.shortUrl = selectSafe(articleJson, "short_url")        // this.images = [:]        // this.pubDate = selectSafe(articleJson, "published_at")    }    */}